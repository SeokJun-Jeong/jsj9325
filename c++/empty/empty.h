//class 기본생성자 

#ifndef EMPTY_H
#define EMPTY_H

class Empty
{
private:
    
public:
    // Empty() { };
    // Empty(const Empty& rhs) {memberwise copy};//
    // ~Empty() { };

    // Empty& operator=(const Empty& rhs) { memberwise copy  return this;}

    // Empty* operator&() {return this; }
    // const Empty* operator&() const { return this; } //15,16번 함수 중복  const 사용하여 같은 함수이름과 인자의 타입, 인자의 갯수가 같아도 가능
    
};


#endif